services:
  - type: web
    name: going-org
    runtime: node
    plan: starter
    buildCommand: npm run build
    startCommand: npm start
    disk:
      name: going-data
      mountPath: /opt/render/project/src/uploads
      sizeGB: 1
    envVars:
      - key: NODE_ENV
        value: production
