services:
  - type: web
    name: going-org
    runtime: node
    nodeVersion: "20.11.0"
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
